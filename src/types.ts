export type ApiCharacter = {
  id: number;
  name: string;
  status: string;
  species: string;
  type: string;
  gender: string;
  created: string;
  origin: string;
  location: string;
};

export type Character = {
  id: number;
  name: string;
  description: string;
};
